@model Specification

@Html.HiddenFor(m => m.Id)
<div class="grid">
   <div class="col-12">@Html.EditorFor(m => m.Name)</div>
   <div class="col-12">@Html.EditorFor(m => m.Service)</div>
</div>

<script>
   $(function () {
      @(Model.Id == 0 ? "Name.Focus();": "")
   });
</script>