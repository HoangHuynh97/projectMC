@model ReportViewer.ReportParameters
@Html.HiddenFor(m => m.Id)
<div class="mb">
   <h3 style="margin: 0; padding: 0;">@Model.Name</h3>
</div>
@foreach (var p in Model.Parameters)
{
   <div class="mb">
      @Html.EditorFor(m => p, "", "")
   </div>
}