@model CompanyUser.ChangePassword
@Html.HiddenFor(m => m.Id)
<div class="grid">
   <div class="col-12">
      @Html.EditorFor(m => m.Password)
   </div>
   <div class="col-12">
      @Html.EditorFor(m => m.Verify)
   </div>
</div>